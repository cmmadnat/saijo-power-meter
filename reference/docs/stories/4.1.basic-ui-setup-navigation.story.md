# <!-- Powered by BMAD™ Core -->
# Story 4.1: Basic UI Setup and Navigation

## Status
Approved

## Story
**As a** user,
**I want** to have a basic UI with a clear navigation structure,
**so that** I can easily navigate between the different modules.

## Acceptance Criteria
1. A basic layout is created with a header, sidebar navigation, and content area.
2. The sidebar navigation contains links to the three modules: Calorie Meter Room, EMC, and Function Test.
3. The routing is set up to display the correct view when a navigation link is clicked.
4. The UI is responsive and works well on desktop browsers.

## Tasks / Subtasks
- [x] Task 1 (AC: #1)
  - [x] Set up React project (existing project enhanced with Material-UI).
  - [x] Install Material-UI (MUI).
  - [x] Create a `Layout` component with header, sidebar, and content areas.
- [x] Task 2 (AC: #2)
  - [x] Create navigation within `Layout` component.
  - [x] Add navigation links for the three modules.
- [x] Task 3 (AC: #3)
  - [x] Install `react-router-dom` (already present).
  - [x] Configure routes for each module view.
- [x] Task 4 (AC: #4)
  - [x] Use MUI's responsive design for layout and components.

## Dev Notes
- The `architecture.md` specifies using TypeScript, React, and Material-UI. The developer must adhere to this stack.
- Remember the location contradiction for the frontend code (`src/main/frontend` vs. `admin-ui`). This needs to be resolved before starting.

### Testing
- Component tests should be written for the `Layout` and `Sidebar` components.

## Change Log
| Date | Version | Description | Author |
| :--- | :--- | :--- | :--- |
| 2025-08-30 | 1.0 | Initial draft | Sarah |

## Dev Agent Record
### Agent Model Used
{{agent_model_name_version}}
### Debug Log References
-
### Completion Notes List
-
### File List
-

## QA Results
-
